class Solution {
    public int find(int[] arr) {
        long need = 0L;
        for (int i = arr.length - 1; i >= 0; --i) {
            long sum = need + arr[i];
            // ceil(sum / 2) for non-negative integers:
            need = (sum + 1) / 2;
        }
        return (int) need;
    }
}
