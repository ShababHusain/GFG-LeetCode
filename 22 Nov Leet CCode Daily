class Solution {
    public int minimumOperations(int[] nums) {
        int ops = 0;
        for (int v : nums) {
            int r = v % 3;
            if (r == 0) continue;
            ops += Math.min(r, 3 - r);
        }
        return ops;
    }
}
