class Solution {
    public boolean isPower(int x, int y) {
        if (x == 1) return y == 1;
        if (y == 0) return false; // not needed by constraints but safe
        while (y % x == 0) {
            y /= x;
        }
        return y == 1;
    }
}
