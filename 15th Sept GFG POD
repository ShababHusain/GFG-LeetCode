import java.util.*;

/*
class Node {
    int data;
    Node left, right;
    Node(int d) {
        data = d;
        left = right = null;
    }
}
*/

class Solution {
    public int getCount(Node root, int k) {
        List<Integer> leafLevels = new ArrayList<>();
        collectLeafLevels(root, 1, leafLevels);

        // Sort levels in ascending order to pick the cheapest leaves first
        Collections.sort(leafLevels);

        int count = 0;
        for (int level : leafLevels) {
            if (k >= level) {
                k -= level;
                count++;
            } else {
                break; // Remaining leaves exceed budget
            }
        }

        return count;
    }

    private void collectLeafLevels(Node node, int level, List<Integer> leafLevels) {
        if (node == null) {
            return;
        }

        // Check if current node is a leaf
        if (node.left == null && node.right == null) {
            leafLevels.add(level);
            return;
        }

        collectLeafLevels(node.left, level + 1, leafLevels);
        collectLeafLevels(node.right, level + 1, leafLevels);
    }
}
